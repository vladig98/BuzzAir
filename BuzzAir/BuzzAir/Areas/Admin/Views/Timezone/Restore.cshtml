@model RestoreTimezoneVM
@{
    ViewData["Title"] = "Restore Timezone";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-info shadow-sm">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0">Restore Timezone</h5>
                </div>
                <div class="card-body">
                    <form asp-area="Admin" asp-controller="Timezone" asp-action="Restore" method="post" class="row g-3">
                        @* hide the Id *@
                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="Name" />
                        <input type="hidden" asp-for="Identifier" />
                        <input type="hidden" asp-for="Abbreviation" />
                        <input type="hidden" asp-for="Offset" />
                        <input type="hidden" asp-for="UsesDST" />

                        <div class="col-12 form-floating">
                            <input asp-for="Name" class="form-control disabled" disabled readonly />
                            <label asp-for="Name">Timezone</label>
                        </div>

                        <div class="col-md-6 form-floating">
                            <input asp-for="Identifier" class="form-control disabled" disabled readonly />
                            <label asp-for="Identifier">Identifier</label>
                        </div>

                        <div class="col-md-6 form-floating">
                            <input asp-for="Abbreviation" class="form-control disabled" disabled readonly />
                            <label asp-for="Abbreviation">Abbreviation</label>
                        </div>

                        <div class="col-md-6 form-floating">
                            <input asp-for="Offset" class="form-control disabled" disabled readonly />
                            <label asp-for="Offset">Offset</label>
                        </div>

                        <div class="col-md-6 form-check form-switch">
                            <input asp-for="UsesDST" class="form-check-input disabled" disabled type="checkbox" />
                            <label asp-for="UsesDST" class="form-check-label disabled" disabled>Uses DST?</label>
                        </div>

                        <div class="col-12 text-end">
                            <button type="submit" class="btn btn-success">
                                Restore
                            </button>
                            <a class="btn btn-secondary" asp-area="Admin" asp-controller="Timezone" asp-action="Deleted" asp-route-pageNumber="1">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
